# dunst theme colors -- auto-generated from colors.toml
[global]
    frame_color = "{{ accent }}"

[urgency_low]
    background = "{{ surface }}"
    foreground = "{{ foreground }}"
    frame_color = "{{ muted }}"

[urgency_normal]
    background = "{{ background }}"
    foreground = "{{ foreground }}"
    frame_color = "{{ accent }}"

[urgency_critical]
    background = "{{ danger }}"
    foreground = "{{ foreground }}"
    frame_color = "{{ danger }}"
