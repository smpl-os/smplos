# dunst notification daemon -- core settings
# Theme colors are appended via [global] overrides from theme's dunstrc.theme
[global]
    monitor = 0
    follow = mouse
    width = 350
    height = 120
    origin = top-right
    offset = 8x8
    notification_limit = 5
    indicate_hidden = yes
    padding = 12
    horizontal_padding = 12
    frame_width = 2
    corner_radius = 8
    font = JetBrainsMono Nerd Font 10
    icon_position = left
    max_icon_size = 48
    show_indicators = no
    mouse_left_click = do_action, close_current
    mouse_right_click = close_all
    default_icon = dialog-information
    enable_recursive_icon_lookup = true

[urgency_low]
    timeout = 5

[urgency_normal]
    timeout = 5

[urgency_critical]
    timeout = 0

# ── OSD-style notifications: show on screen, never enter history ─────────────
# Volume and Brightness key presses fire notifications that act as on-screen
# displays (slider with int:value hint). They should NOT pollute notification
# history -- otherwise scrolling back through real notifications becomes
# painful. `history_ignore = yes` makes dunst drop them on timeout instead
# of archiving them. Matched by summary because notify-send doesn't let us
# set a stable appname (it's always "notify-send").
[osd_volume]
    summary = "Volume"
    history_ignore = yes
    skip_display = no

[osd_brightness]
    summary = "Brightness"
    history_ignore = yes
    skip_display = no
