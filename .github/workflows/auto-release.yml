name: Auto Release

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  bump:
    uses: smpl-os/.github/.github/workflows/auto-release.yml@main
    with:
      version-file: src/VERSION
      branch: main
    secrets: inherit
